# AllNodes

This is init file where are all multi-node things. You'll have to init All nodes the same way. Run ./init.sh, you can run it with argument main (RP4/5) or side (RP zero).

## TODOs

- Make manager.py, buildConstruct.py and RWDdata.py into one folder galena (this change will have to change how buildConstruct takes constructs, galena service has to be changed and maybe other things).
