{
    "run" : {
        "type" : "never"
    },
    "initRun" : [
        "sudo apt update > /dev/null && sudo apt upgrade > /dev/null",
        "sudo apt install -y sqlite3 docker.io > /dev/null",
        "sudo mkdir -p /mnt/forgejo",
        "sudo docker run -d --name forgejo -p 7000:3000 -v /mnt/forgejo:/data --restart unless-stopped codeberg.org/forgejo/forgejo:16.0.2"
    ],
    "boot" : [
    ],
    "data" : {

    }
}